set(WEB_TEST_LIBS
    http_client_lib
    ws_lib
    ws_common_lib
    base_lib
    process_lib
    fs_lib
    stats_lib
    time_lib
)

nebula_add_test(
    NAME
        flags_access_test
    SOURCES
        FlagsAccessTest.cpp
    LIBRARIES
        ${WEB_TEST_LIBS}
        proxygenhttpserver
        proxygenlib
        wangle
        gtest
)


nebula_add_test(
    NAME
        stats_read_test
    SOURCES
        StatsReaderTest.cpp
    LIBRARIES
        ${WEB_TEST_LIBS}
        proxygenhttpserver
        proxygenlib
        wangle
        gtest
)

nebula_add_test(
    NAME
        router_test
    SOURCES
        RouterTest.cpp
    LIBRARIES
        ${WEB_TEST_LIBS}
        proxygenhttpserver
        proxygenlib
        wangle
        gtest
)

nebula_add_test(
    NAME
        http_status_test
    SOURCES
        StatusHandlerTest.cpp
    LIBRARIES
        ${WEB_TEST_LIBS}
        proxygenhttpserver
        proxygenlib
        wangle
        gtest
)
